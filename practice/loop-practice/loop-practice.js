const num = [0, 1, 2, 3, 4, 5];

for (let i = 0; i < num.length; i++) {
    console.log(num[i]);
    
    // let output = num[i];
    // document.getElementById('para').innerHTML = output;

    // document.getElementById('para').innerHTML = num[i];
}